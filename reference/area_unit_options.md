# Area units (vector)

A vector of supported area units derived from `dist_units` and
[`units::valid_udunits()`](https://r-quantities.github.io/units/reference/valid_udunits.html).

## Usage

``` r
area_unit_options
```

## Format

A character vector with 41 names, plural names, and aliases for area
units.
