# Distance units (vector)

A vector of supported distance units pulled from `dist_units`.

## Usage

``` r
dist_unit_options
```

## Format

A character vector with 86 names, plural names, aliases, and symbols for
distance units.
